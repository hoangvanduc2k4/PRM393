// firebase_options.dart — Firebase đã được loại bỏ khỏi dự án.
// File này được giữ lại để tránh lỗi import cũ nếu còn tồn tại.
class DefaultFirebaseOptions {
  // Deprecated — Firebase no longer used.
}
